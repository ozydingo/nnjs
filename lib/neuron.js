neuron = function(num_inputs) {
  var num_inputs;
  var weights;
  var activation_fn;

  function forward(inputs) {
    return null;
  }

  function backward() {
    return null;
  }

  return {
    forward: forward,
    backward: backward
  };
}
